server:
  port: 8180
eureka:
  instance:
    hostname: 127.0.0.1
  client:
    # 向注册中心注册自己
    register-with-eureka: true
    # 注册中心检索服务
    fetch-registry: true
    service-url:
      defaultZone: http://127.0.0.1:8181/eureka/,http://127.0.0.1:8180/eureka/
